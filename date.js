// //jshint esversion:6

// //module.exports.getDate = getDate; // don't add parenthese now because we don't want our function to run now add parenthese on app.js page on date()

// //module.exports is same as just exports so we can have a shortcut
// exports.getDate = function(){
//     const today = new Date();
    
//     const options = {
//         weekday: "long",
//         day: "numeric",
//         month: "long",
//     };

//     return today.toLocaleDateString("en-US", options);
// }

// exports.getDay = function(){
//     const today = new Date();
    
//     const options = {
//         weekday: "long",
//     };

//     return today.toLocaleDateString("en-US", options);
// }

// console.log(module.exports);